package oop_java.object_factory;
//An object factory is a design pattern that provides a way to create objects without exposing the creation logic to the calling code.

public interface Shape {
    void draw();
}